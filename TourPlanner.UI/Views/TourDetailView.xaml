<UserControl x:Class="TourPlanner.UI.Views.TourDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:TourPlanner.UI.Views.Components"
             xmlns:converters="clr-namespace:TourPlanner.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <TabControl SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}" Margin="0,0,0,16">
            <TabItem Header="General" Height="32">
                <StackPanel Visibility="{Binding SelectedTour, Converter={StaticResource NullToVisibilityConverter}}" Margin="0,0,0,16">
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.Id, StringFormat='Identifier: {0}'}" Margin="0,4,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.Name, StringFormat='Name: {0}'}" Margin="0,4,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.Description, StringFormat='Description: {0}'}" Margin="0,4,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.StartLocation, StringFormat='Start-Location: {0}'}" Margin="0,4,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.EndLocation, StringFormat='End-Location: {0}'}" Margin="0,4,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.TransportType, StringFormat='Transport-Type: {0}'}" Margin="0,4,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.DistanceKm, StringFormat='Distance: {0}km'}" Margin="0,4,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding SelectedTour.EstimatedTimeHours, StringFormat='Estimated-Hours: {0}'}" Margin="0,4,0,0"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="Route">
                <components:MapView />
            </TabItem>
            <TabItem Header="Misc.">
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Text="- The Weather will be displayed here -" Margin="0,16"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>