<UserControl x:Class="TourPlanner.UI.Views.TourNavbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.UI.Views"
             mc:Ignorable="d" 
             Height="60" Background="#1F2937">

    <DockPanel VerticalAlignment="Top" Height="60" LastChildFill="True">
        <!-- App Title -->
        <TextBlock Text="TourPlanner"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="White"
                   VerticalAlignment="Center"
                   Margin="20,0,0,0"
                   DockPanel.Dock="Left" />

        <!-- Navigation Buttons -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,20,0"
                    DockPanel.Dock="Right">
            <Menu>
                <MenuItem Header="_File">
                    <MenuItem Header="New Tour" InputGestureText="Strg+N" Command="{Binding FileCommand}" />
                    <MenuItem Header="New Tour-Log" InputGestureText="Strg+Umschalt+N" Command="{Binding NewWindowCommand}" />
                    <Separator />
                    <MenuItem Header="Import..." InputGestureText="Strg+O" Command="{Binding OpenCommand}" />
                    <MenuItem Header="Export..." InputGestureText="Strg+S" Command="{Binding SaveCommand}" />
                    <Separator />
                    <MenuItem Header="Quit" Command="{Binding ExitCommand}" />
                </MenuItem>
                <MenuItem Header="_Edit">
                    <MenuItem Header="Neue Registerkarte" InputGestureText="Strg+N" Command="{Binding NewTabCommand}" />
                    <MenuItem Header="Neues Fenster" InputGestureText="Strg+Umschalt+N" Command="{Binding NewWindowCommand}" />
                    <Separator />
                    <MenuItem Header="Öffnen" InputGestureText="Strg+O" Command="{Binding OpenCommand}" />
                    <MenuItem Header="Speichern" InputGestureText="Strg+S" Command="{Binding SaveCommand}" />
                    <MenuItem Header="Speichern unter..." InputGestureText="Strg+Umschalt+S" Command="{Binding SaveAsCommand}" />
                    <Separator />
                    <MenuItem Header="Beenden" Command="{Binding ExitCommand}" />
                </MenuItem>
                <MenuItem Header="_View">
                    <MenuItem Header="Tour-List" InputGestureText="Strg+N" Command="{Binding NewTabCommand}" />
                    <MenuItem Header="Detail-View" InputGestureText="Strg+Umschalt+N" Command="{Binding NewWindowCommand}" />
                    <MenuItem Header="Tour-Log-List" InputGestureText="Strg+Umschalt+N" Command="{Binding NewWindowCommand}" />
                </MenuItem>
                <MenuItem Header="_Help">
                    <MenuItem Header="About..." InputGestureText="Strg+N" Command="{Binding NewTabCommand}" />
                    <MenuItem Header="How to use..." InputGestureText="Strg+Umschalt+N" Command="{Binding NewWindowCommand}" />
                    <Separator />
                    <MenuItem Header="Visit GitHub-Repository" InputGestureText="Strg+Umschalt+N" Command="{Binding NewWindowCommand}" />
                </MenuItem>
            </Menu>
        </StackPanel>
    </DockPanel>
</UserControl>
