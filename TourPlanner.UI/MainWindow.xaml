<Window x:Class="TourPlanner.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:TourPlanner.UI.Views"
        xmlns:local="clr-namespace:TourPlanner.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1280">
    <Grid Margin="20,20,20,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- TourList -->
        <Label Grid.Column="0" Content="Nav-Placeholder"  Margin="10,10,10,10"/>

        <!-- Tour -->
        <views:TourDetailView Grid.Column="1"/>

        <!-- TourLogs -->
        <views:TourLogsView Grid.Column="2"/>
    </Grid>
</Window>
